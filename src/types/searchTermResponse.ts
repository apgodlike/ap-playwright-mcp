export interface SearchTermResponse {
  store: string;
  product: string;
  id: string;
  price_per_gram: number;
  brand_name: string;
  absolute_url: string;
  magnitude: string;
  price: string;
  quantity: string;
}
